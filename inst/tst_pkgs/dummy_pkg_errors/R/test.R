#' @keywords internal
tstCharLimitLintr <- function() {
  x   <- 1 # workaround for GDR-1234
  print("This file is simply a test to see whether or not the lintr functions appropriately detect lines that are too long.")
}
