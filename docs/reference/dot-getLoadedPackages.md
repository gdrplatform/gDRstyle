# Collect package names from library()/require() calls in code

Collect package names from library()/require() calls in code

## Usage

``` r
.getLoadedPackages(code)
```

## Arguments

- code:

  string R code (may span multiple chunks).

## Value

character vector of package names.
