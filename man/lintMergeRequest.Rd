% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contribution_linter.R
\name{lintMergeRequest}
\alias{lintMergeRequest}
\title{Lint a merge/pull request title and body}
\usage{
lintMergeRequest(title, body, pkg_dir = ".", branch = NULL)
}
\arguments{
\item{title}{character(1) merge/pull request title.}

\item{body}{character(1) merge/pull request description.}

\item{pkg_dir}{character(1) path to the repository root used to locate the
PR/MR template. Defaults to the current directory.}

\item{branch}{character(1) source branch name, or \code{NULL} to skip the
branch-name check. Defaults to \code{NULL}.}
}
\value{
\code{NULL} invisibly if no violations are found. Stops with an
error otherwise.
}
\description{
Convenience wrapper for CI: checks the title for ticket references and, when
the repository ships a PR/MR template, checks that the body keeps its
sections. The template check is skipped (with a message) when no template is
present, so repositories without one are not blocked.
}
\examples{
lintMergeRequest("feat: add contribution linter", "details", tempdir())

}
\keyword{linter}
